#include "bluetooth.h"

void bluetooth_init(const char* name) {
  // TODO init
}

bool bluetooth_write(char* buf, int len) {
  // TODO write (raw, no transcoder)
}

int bluetooth_read(char* &buf) {
  // TODO read (raw, no transcoder)
}
